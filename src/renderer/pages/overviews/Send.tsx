import { Typography , Button, Card, Divider , Statistic , Row , Col , Modal } from 'antd';
import { useEffect } from 'react';


const { Title } = Typography;

export default () => {
  return (<>
        <Title level={4} style={{marginTop:"0px"}}>Send</Title>
    <Divider />
  </>)
}
